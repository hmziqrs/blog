---
// Only rendered when Firebase measurement ID is configured
const hasMeasurementId = Boolean(import.meta.env.PUBLIC_FIREBASE_MEASUREMENT_ID);
if (!hasMeasurementId) return;
---

<div
  id="consent-banner"
  class="fixed bottom-0 left-0 right-0 z-50 hidden bg-base-200 border-t border-base-content/10 px-4 py-3"
  role="dialog"
  aria-labelledby="consent-title"
>
  <div class="max-w-4xl mx-auto flex flex-col sm:flex-row items-start sm:items-center gap-3 justify-between">
    <p id="consent-title" class="text-sm text-base-content/80">
      This site uses analytics to understand how content performs.
      No personal data is sold or shared.
    </p>
    <div class="flex gap-2 shrink-0">
      <button id="consent-decline" class="btn btn-ghost btn-xs">Decline</button>
      <button id="consent-accept" class="btn btn-primary btn-xs">Accept</button>
    </div>
  </div>
</div>

<script is:inline>
  (function () {
    const CONSENT_KEY = "analytics_consent";
    const banner = document.getElementById("consent-banner");
    if (!banner) return;

    const stored = localStorage.getItem(CONSENT_KEY);

    if (stored === null) {
      banner.classList.remove("hidden");
    } else if (stored === "true") {
      window.__initAnalytics?.();
    }

    document.getElementById("consent-accept")?.addEventListener("click", function () {
      localStorage.setItem(CONSENT_KEY, "true");
      banner.classList.add("hidden");
      window.__initAnalytics?.();
    });

    document.getElementById("consent-decline")?.addEventListener("click", function () {
      localStorage.setItem(CONSENT_KEY, "false");
      banner.classList.add("hidden");
    });
  })();
</script>
